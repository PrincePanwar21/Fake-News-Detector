# Fake-News-Detector
A machine learning–based Fake News Detector built using Python and Scikit-learn. The model uses TF-IDF vectorization and Logistic Regression to classify news headlines or text as real or fake. It is trained on a labeled news dataset and allows users to input custom news text for prediction.
